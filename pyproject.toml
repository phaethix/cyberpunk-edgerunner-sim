[tool.ruff]
line-length = 100
target-version = "py39"
exclude = [".venv", "__pycache__", "server/tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["server/tests"]
pythonpath = ["."]
