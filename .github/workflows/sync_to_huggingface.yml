name: Sync to Hugging Face Spaces

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Push to Hugging Face
      env:
        HF_REPO: "huggingface.co/spaces/cosmoli/cyberpunk-edgerunner-sim"
      run: |
        git push --force https://cosmoli:${{ secrets.HF_TOKEN }}@$HF_REPO main
